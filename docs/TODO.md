# TODO.md — navaport-kiosk

> Статус: **rolling**. Здесь только открытые задачи; завершённые — в `CHANGELOG.md`.
>
> Формат: `- [ ]` открыто. Завершённое переносить в `CHANGELOG.md` и убирать отсюда.

---

## Перед финальной упаковкой (Фаза 5)

- [ ] Заполнить `tours/` бандлами panotour; раскомментировать строку `tours` в `extraResources` (`electron-builder.json5`)
- [ ] Добавить `build-resources/icon.ico` (256×256) перед вызовом `npm run dist`
- [ ] Убрать fallback-кнопку «‹» из `TourScreen` после реализации `TOUR_EXIT` в panotour

---

## Открытые решения

- [ ] **D4. Источник SVG-планов этажей.** Пока используется placeholder `floor-1.svg`. Заменить на реальный план.
- [ ] **D5. Готовность туров.** Сколько локаций реально готовы; чем закрывать листья без тура (инфо-карточка).
- [ ] **D6. Несколько терминалов?** Если да — контекст-переключатель рядом с языком (без добавления уровня меню).
